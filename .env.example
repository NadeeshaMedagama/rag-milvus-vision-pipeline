# ============================================================================
# Azure OpenAI Configuration
# ============================================================================
# Get your credentials from: https://portal.azure.com/
AZURE_OPENAI_API_KEY=your_azure_openai_api_key_here
AZURE_OPENAI_ENDPOINT=https://your-resource-name.openai.azure.com/
AZURE_OPENAI_DEPLOYMENT_NAME=your-deployment-name
AZURE_OPENAI_API_VERSION=2024-12-01-preview
AZURE_OPENAI_EMBEDDING_DEPLOYMENT=your-embedding-deployment-name

# ============================================================================
# Milvus Cloud Configuration
# ============================================================================
# Get your credentials from: https://cloud.zilliz.com/
MILVUS_URI=https://your-instance.zillizcloud.com:19530
MILVUS_TOKEN=your_milvus_token_here
MILVUS_COLLECTION_NAME=readme_embeddings

# ============================================================================
# GitHub Repository Configuration
# ============================================================================
GITHUB_REPO_URL=https://github.com/YourUsername/YourRepository.git
GITHUB_TOKEN=your_github_token_here  # Optional: for private repos

# ============================================================================
# Application Configuration
# ============================================================================
CHUNK_SIZE=1000           # Size of text chunks (characters)
CHUNK_OVERLAP=200         # Overlap between chunks (characters)
EMBEDDING_DIMENSION=1536  # Dimension for text-embedding-ada-002 (don't change)

# ============================================================================
# Google Vision API Configuration
# ============================================================================
# Setup guide: docs/GOOGLE_VISION_SETUP.md
# 1. Create Google Cloud project: https://console.cloud.google.com/
# 2. Enable Cloud Vision API
# 3. Create service account and download JSON credentials
# 4. Place the JSON file in ./credentials/ directory
# 5. Update the path below to match your credentials file name

GOOGLE_APPLICATION_CREDENTIALS=./credentials/your-google-credentials.json

# GOOGLE_VISION_MAX_RESULTS: Number of results to return from Vision API
# - Controls how many labels, objects, logos to detect per image
# - Higher = more detailed analysis but slower processing
# - Recommended values:
#   * 5  = Fast, basic analysis
#   * 10 = Balanced (RECOMMENDED)
#   * 20 = Detailed, slower
GOOGLE_VISION_MAX_RESULTS=10

# ============================================================================
# Local Data Directory Configuration
# ============================================================================
# Path to directory containing local files (diagrams, images, docs)
DATA_DIRECTORY=./data/diagrams

# PROCESS_LOCAL_FILES: Enable/disable local file processing
# - true  = Process images, diagrams, Word docs, spreadsheets
# - false = Only process markdown files from GitHub
PROCESS_LOCAL_FILES=true

# ============================================================================
# Processing Control Configuration
# ============================================================================
# SKIP_EXISTING_DOCUMENTS: Skip documents already in vector database
# - true  = INCREMENTAL MODE - Only process new/changed documents (RECOMMENDED)
# - false = FULL REINDEX - Process all documents every time
#
# Benefits of INCREMENTAL MODE (true):
# ✅ Faster processing (skips already indexed files)
# ✅ Saves API costs (no re-embedding of existing docs)
# ✅ Preserves existing embeddings
# ✅ Perfect for adding new diagrams/docs
#
# Use FULL REINDEX (false) when:
# - First time setup
# - Changed chunking parameters (CHUNK_SIZE, CHUNK_OVERLAP)
# - Want to rebuild entire index from scratch
SKIP_EXISTING_DOCUMENTS=true

# FORCE_REPROCESS: Override SKIP_EXISTING_DOCUMENTS and reprocess everything
# - true  = FORCE FULL REINDEX - Reprocess all documents (deletes existing collection)
# - false = Respect SKIP_EXISTING_DOCUMENTS setting (RECOMMENDED)
#
# ⚠️  WARNING: Setting this to 'true' will:
#     - Delete all existing embeddings
#     - Reprocess all documents
#     - Use Azure OpenAI API credits for re-embedding
#     - Take longer to complete
#
# Use FORCE_REPROCESS=true only when:
# - You changed embedding model
# - You want to completely rebuild the index
# - You're troubleshooting issues
FORCE_REPROCESS=false

# ============================================================================
# Quick Reference - Common Scenarios
# ============================================================================
#
# 📌 SCENARIO 1: First Time Setup
#    SKIP_EXISTING_DOCUMENTS=true
#    FORCE_REPROCESS=false
#    → Creates new collection and processes all documents
#
# 📌 SCENARIO 2: Adding New Diagrams (RECOMMENDED FOR DAILY USE)
#    SKIP_EXISTING_DOCUMENTS=true
#    FORCE_REPROCESS=false
#    → Only processes new files, skips existing ones ✅ FAST!
#
# 📌 SCENARIO 3: Changed Chunk Size/Overlap
#    SKIP_EXISTING_DOCUMENTS=false
#    FORCE_REPROCESS=true
#    → Deletes and rebuilds entire index
#
# 📌 SCENARIO 4: Testing/Development
#    SKIP_EXISTING_DOCUMENTS=false
#    FORCE_REPROCESS=true
#    → Full reprocess every time (useful for testing)
#
# ============================================================================

